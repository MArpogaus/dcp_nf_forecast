 I want to start a new forecasting project using my multivariate normalizing flows. Data can be found in ../dcp/norm_data/norm_fondium_15_min_data_2023.csv. A Previous forecasting project is in ../ai4grids/bnf_scenario_generator/. Pleas helpt mi initialize the project in a similar style. lets begin by layoing out the basic file structure ang generating a script to load and prepare the data. it should read in the csv data in generate two seperate data frame one conating the covaraites and one conatining the target variables. it should also use cos/sin embedings for circular variables like time and date. This is different from the previous project where we used a external package for thet. we want a palin pyton file here to generat the csv files for us. a second pyton script should then be used to split thes into train, validation and test splits. all hyperparameters and configuration should be put in params.yam including all file paths, splitsizes. column names for x and y. pleas begin by laing aout a plan to setup the project and draft these two scripts. thy should be later included in a dvc pipline (dvc.yaml). draft this one as well
